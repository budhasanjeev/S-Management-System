package stocky

import grails.transaction.Transactional

@Transactional
class StakeValueService {
/*
    def stakeValueSave(params){
        def stake = new Stake(params)
        stake.stake_value= params.stake
        stake.date_created= new Date()
        stake.date_updated = new Date()

        if (stake.save(flush: true))
        {
            return true
        }
    }
}*/
}